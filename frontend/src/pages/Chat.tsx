import React from "react";

const Chat: React.FC = () => {
  return (
    <div className="container py-6">
      <div className="bg-white p-6 rounded shadow">
        <h2 className="text-xl font-semibold">Chat (prototype)</h2>
        <p>Chat will be implemented here (Django Channels WebSocket upgrade planned).</p>
      </div>
    </div>
  );
};

export default Chat;
